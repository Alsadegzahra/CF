/** Recharts colors — light UI, lime accent */
export const chartColors = {
  accent: "#84cc16",
  accentSoft: "rgba(132, 204, 22, 0.22)",
  grid: "rgba(15, 23, 42, 0.08)",
  axis: "#64748b",
  tooltipBg: "#ffffff",
  tooltipBorder: "#e2e8f0",
  peak: "#eab308",
  highlight: "#22c55e",
};

/** Distinct series for the four players (Analysis timeline / legends). */
export const playerSeriesColors = ["#1e3a8a", "#65a30d", "#0f766e", "#b45309"] as const;
